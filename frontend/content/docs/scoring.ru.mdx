---
title: Как рассчитывается оценка
description: Посмотрите оценку 0–100, класс, состояние поддержки и воспроизводимую публичную модель каждого артефакта.
---

У каждой записи есть оценка 0–100, буквенный класс и состояние поддержки. Таблица ниже напрямую отображает модель из `GET /api/v1/scoring`.

<ScoringModel />
