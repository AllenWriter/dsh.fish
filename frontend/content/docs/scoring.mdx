---
title: How the score works
description: Every listing carries a 0–100 score, a letter grade and a maintenance status. This page renders the model GET /api/v1/scoring serves.
---

Every listing carries a 0–100 score, a letter grade and a maintenance status. The tables below render the model exactly as `GET /api/v1/scoring` serves it — what you read here is what the registry computes.

<ScoringModel />
