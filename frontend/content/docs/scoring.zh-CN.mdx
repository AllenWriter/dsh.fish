---
title: 评分如何计算
description: 为什么每个条目都带有 0–100 分、字母等级和维护状态，以及评分模型为什么以数据形式公开。
---

安装的插件会带着你的权限运行，所以安装前你会想知道：有没有人在用它、还有没有人维护、它敢不敢亮出自己的凭据。评分把这三个问题对应成三个维度——人气、维护、质量——再合成一个 0–100 分、一个字母等级和一个维护状态。

下面的表格完全按 `GET /api/v1/scoring` 返回的模型渲染，你读到的就是注册表实际计算的规则。

<ScoringModel />
